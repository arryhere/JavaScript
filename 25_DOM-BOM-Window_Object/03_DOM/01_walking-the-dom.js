/*
• DOM tree refers to the HTML page where all the nodes are Obects.
• There can be three main types of nodes in the DOM tree : 
    text nodes
    element nodes 
    comment nodes
• 
• 
*/

// head tag
console.log('head', document.head);

// body tag
// documennt.body can sometimes be null if js is written before body tag
console.log('body', document.body);

// page HTML tag
console.log('documentElement', document.documentElement);

// title
console.log('title', document.title);
